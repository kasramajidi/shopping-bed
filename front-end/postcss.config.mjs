/** @type {import('postcss-load-config').Config} */
const config = {
    plugins: {
      '@tailwindcss/postcss': {},  // تغییر به @tailwindcss/postcss
      autoprefixer: {},    
    },
  };
  
  export default config;
  